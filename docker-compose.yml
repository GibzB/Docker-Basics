version: '3.7'

services:
  postgres:
    image: postgres:11.8
    environment:
      - POSTGRES_PASSWORD=123
    volumes:
      - postgresql-data:/var/lib/postgresql/data
    
    ports:
      - "5432:5432"
volumes:
  postgresql-data:
    
